"""Stable error codes, returned as `{code, message, detail}` (docs/api-spec.md).

The frontend translates codes; the sidecar never returns user-facing prose (D17).
Populated from Session 1 onward.
"""
